<!doctype html>
<html class="no-js" lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="">
    <link rel="canonical" href="{{ canonical_url }}">
    <meta name="robots" content="index, follow">
    {%- if settings.favicon != blank -%}
      <link rel="icon" type="image/png" href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {%- endif -%}

    {%- unless settings.type_header_font.system? and settings.type_body_font.system? -%}
      <link
        rel="preconnect"
        href="https://fonts.shopifycdn.com"
        crossorigin
      >
    {%- endunless -%}

    <title>
      {{ page_title }}
      {%- if current_tags %}
        &ndash; tagged "{{ current_tags | join: ', ' }}"
      {%- endif -%}
      {%- if current_page != 1 %}
        &ndash; Page {{ current_page -}}
      {%- endif -%}
      {%- unless page_title contains shop.name %}
        &ndash; {{ shop.name -}}
      {%- endunless -%}
    </title>

    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {% render 'meta-tags' %}

    {{ content_for_header }}
    {% render 'context-style-variables' %}

    {% comment %}
      {{ 'swiper-bundle.min.css' | asset_url | preload_tag: as: 'style' }}
      {{ 'animate.css' | asset_url | preload_tag: as: 'style' }}
      {{ 'base.css' | asset_url | preload_tag: as: 'style' }}
      {{ 'component.css' | asset_url | preload_tag: as: 'style' }}
      {{ 'theme.css' | asset_url | preload_tag: as: 'style' }}
      {{ 'account.css' | asset_url | preload_tag: as: 'style' }}
    {% endcomment %}

    {{ 'swiper-bundle.min.css' | asset_url | stylesheet_tag }}
    {{ 'animate.css' | asset_url | stylesheet_tag }}
    {{ 'base.css' | asset_url | stylesheet_tag }}
    {% comment %} <link rel="stylesheet" href="{{ 'component.css' | asset_url }}" onload="this.media='all'"> {% endcomment %}
    {{ 'component.css' | asset_url | stylesheet_tag }}
    {{ 'theme.css' | asset_url | stylesheet_tag }}
    {{ 'account.css' | asset_url | stylesheet_tag }}
    <link
      id="ModelViewerStyle"
      rel="stylesheet"
      href="https://cdn.shopify.com/shopifycloud/model-viewer-ui/assets/v1.0/model-viewer-ui.css"
      media="print"
      onload="this.media='all'"
    >

    {%- unless settings.type_body_font.system? -%}
      <link rel="preload" as="font" href="{{ settings.type_body_font | font_url }}" type="font/woff2" crossorigin>
    {%- endunless -%}
    {%- unless settings.type_heading_font.system? -%}
      <link rel="preload" as="font" href="{{ settings.type_heading_font | font_url }}" type="font/woff2" crossorigin>
    {%- endunless -%}

    {%- if localization.available_countries.size > 1 or localization.available_languages.size > 1 -%}
    {%- endif -%}

    {%- if settings.predictive_search_enabled -%}
    {%- endif -%}

    <script>
      document.documentElement.className = document.documentElement.className.replace('no-js', 'js');
      if (Shopify.designMode) {
        document.documentElement.classList.add('shopify-design-mode');
      }
    </script>
      {{ 'nainai-custom.css' | asset_url | stylesheet_tag }}
  </head>

  <body class="king" id="king-body">
    <a class="skip-to-content-link button" href="#MainContent">
      {{ 'accessibility.skip_to_text' | t }}
    </a>
    <div class="body-main form-input-{{ settings.form_inputs_height }}" id="king-body-main">
      {% sections 'header-group' %}
      <div class="pinned-content">
        <main id="MainContent" class="content-for-layout focus-none" role="main" tabindex="-1">
          {{ content_for_layout }}
        </main>
        {% sections 'footer-group' %}
      </div>
      {% sections 'overlay-group' %}
    </div>
    <ul hidden>
      <li id="a11y-refresh-page-message">{{ 'accessibility.refresh_page' | t }}</li>
      <li id="a11y-new-window-message">{{ 'accessibility.link_messages.new_window' | t }}</li>
    </ul>

    {% render 'popup',
      modal_name: 'localization-form',
      modal_position: 'center',
      modal_size: 'medium',
      popup_color_scheme: settings.popup_color_scheme
    %}
    {% render 'scroll-to-top' %}
    {% render 'context-variables' %}
  </body>
</html>
